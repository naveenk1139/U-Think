import mongoose from 'mongoose'; import College from './src/models/College.js'; mongoose.connect('mongodb://127.0.0.1:27017/u-think').then(async () => { console.log('Total Colleges:', await College.countDocuments()); console.log('Karnataka:', await College.countDocuments({ state: 'Karnataka' })); const sample = await College.findOne({ source: 'AISHE' }); console.log(sample); process.exit(0); });
